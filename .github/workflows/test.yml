name: test
on:
  push:
    branches:
      - main
    tags: 
      - nursinghome
jobs:
  test1:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '16'
      - run: npm install -g bats
      - run: bats -v

